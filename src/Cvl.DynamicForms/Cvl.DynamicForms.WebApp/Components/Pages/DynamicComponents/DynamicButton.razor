@using Cvl.DynamicForms.Core.Models.ContentControls

<RadzenButton Text="@Element.Name"
              Click="OnClick"
              @* Icon="@Element.Icon" ButtonStyle="@Element.Style" *@
              Disabled="@Element.IsReadOnly"
              Style="width: 100%;" />

@code {
    [Parameter] public Button Element { get; set; }

    private void OnClick(MouseEventArgs args)
    {
        if (Element.Action != null)
        {
            // Execute the defined action
            //Element.Action.Invoke();
        }
    }
}
