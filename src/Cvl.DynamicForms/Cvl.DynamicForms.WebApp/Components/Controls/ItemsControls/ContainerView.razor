@    using Cvl.DynamicForms.Core.Models
@    using Cvl.DynamicForms.WebApp.Components.FormElements

@if (Model != null)
{
    <MudContainer>
        @foreach (var element in Model.Children)
        {
            <MudItem>
                <ControlView Model=@element></ControlView>
            </MudItem>
        }
    </MudContainer>
}

@code {
    [Parameter]
    public ItemsControl? Model { get; set; }
}
