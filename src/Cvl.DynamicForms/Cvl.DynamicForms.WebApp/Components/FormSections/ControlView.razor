@using Cvl.DynamicForms.Core.Models
@if (Model.Type == ItemsControlType.Section)
{
    <SectionCollectionView Model=@Model></SectionCollectionView>
} 
else if (Model.Type == ItemsControlType.Column)
{
    <ColumnView Model=@Model></ColumnView>
} 
else if (Model.Type == ItemsControlType.Row)
{
    <RowView Model=@Model></RowView>
}
else if (Model.Type == ItemsControlType.Table)
{
    <TableView Model=@Model></TableView>
}
else if (Model.Type == ItemsControlType.Tabs)
{
    <TabsView Model=@Model></TabsView>
}
else if (Model.Type == ItemsControlType.Tab)
{
    @foreach (var control in Model.Children)
    {
        <ControlView Model=@control></ControlView>
    }
}
@code {
    [Parameter]
    public ItemsControl? Model { get; set; }
}
